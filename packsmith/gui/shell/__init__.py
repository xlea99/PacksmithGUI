"""The application shell (design 4.1): sidebar, workspace, bottom panel, status strip.

The shell is the frame every feature hangs in. It is deliberately separate from the
things it hosts — view tabs, editors, panels — so adding a panel or a tab type is
additive rather than surgery on the window.
"""
