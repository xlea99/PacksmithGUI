[pytest]
# Unit suite lives in tests/. fancy_tests.py at the repo root is a standalone
# integration script (needs a real pack on disk) and is intentionally NOT collected.
testpaths = tests
pythonpath = .
addopts = -v --tb=short
